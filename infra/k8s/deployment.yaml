# ═══════════════════════════════════════════════════════════════════════════════
# Neurex — Kubernetes Deployment Manifests
#
# Ön koşullar:
#   kubectl apply -f namespace.yaml
#   kubectl create secret generic neurex-secrets \
#     --from-literal=jwt-secret=$(openssl rand -base64 64) \
#     --from-literal=db-password=CHANGEME \
#     --from-literal=engine-internal-key=$(openssl rand -hex 32) \
#     -n neurex
# ═══════════════════════════════════════════════════════════════════════════════

---
# Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: neurex
  labels:
    app.kubernetes.io/name: neurex
    environment: production

---
# ── Backend Deployment ────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neurex-backend
  namespace: neurex
  labels:
    app: neurex-backend
    tier: backend
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: neurex-backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: neurex-backend
        tier: backend
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: backend
          image: ghcr.io/neurex-ai/neurex-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: neurex-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: neurex-secrets
                  key: jwt-secret
            - name: ENGINE_INTERNAL_KEY
              valueFrom:
                secretKeyRef:
                  name: neurex-secrets
                  key: engine-internal-key
            - name: REDIS_URL
              value: "redis://neurex-redis:6379/0"
            - name: ENGINE_BASE_URL
              value: "http://neurex-engine:5001"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://neurex-otel-collector:4317"
            - name: OTEL_SERVICE_NAME
              value: "neurex-backend"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "deployment.environment=production"
            - name: LOG_FORMAT
              value: "json"
            - name: CORS_ORIGINS
              value: "https://bgtest.dev"
            - name: DEBUG
              value: "false"
            - name: PROMETHEUS_ENABLED
              value: "true"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]

---
# ── Worker Deployment ─────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neurex-worker
  namespace: neurex
  labels:
    app: neurex-worker
    tier: worker
    version: "1.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: neurex-worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: neurex-worker
        tier: worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: worker
          image: ghcr.io/neurex-ai/neurex-backend:latest
          imagePullPolicy: Always
          command: ["celery", "-A", "app.workers.celery_app", "worker", "--loglevel=info"]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: neurex-secrets
                  key: database-url
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: neurex-secrets
                  key: jwt-secret
            - name: ENGINE_INTERNAL_KEY
              valueFrom:
                secretKeyRef:
                  name: neurex-secrets
                  key: engine-internal-key
            - name: REDIS_URL
              value: "redis://neurex-redis:6379/0"
            - name: ENGINE_BASE_URL
              value: "http://neurex-engine:5001"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://neurex-otel-collector:4317"
            - name: OTEL_SERVICE_NAME
              value: "neurex-worker"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "deployment.environment=production"
            - name: LOG_FORMAT
              value: "json"
            - name: CORS_ORIGINS
              value: "https://bgtest.dev"
            - name: DEBUG
              value: "false"
            - name: PROMETHEUS_ENABLED
              value: "true"
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]

---
# ── Backend Service ───────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: neurex-backend
  namespace: neurex
spec:
  selector:
    app: neurex-backend
  ports:
    - port: 8000
      targetPort: 8000
      name: http

---
# ── Engine Deployment ─────────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neurex-engine
  namespace: neurex
  labels:
    app: neurex-engine
    tier: engine
spec:
  replicas: 1   # Engine stateful (screenshot storage) — tek replika
  selector:
    matchLabels:
      app: neurex-engine
  template:
    metadata:
      labels:
        app: neurex-engine
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: engine
          image: ghcr.io/neurex-ai/neurex-engine:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5001
              name: http
          env:
            - name: ENGINE_PORT
              value: "5001"
            - name: HEADLESS
              value: "true"
            - name: ENGINE_INTERNAL_KEY
              valueFrom:
                secretKeyRef:
                  name: neurex-secrets
                  key: engine-internal-key
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://neurex-otel-collector:4317"
            - name: OTEL_SERVICE_NAME
              value: "neurex-engine"
            - name: LOG_FORMAT
              value: "json"
          volumeMounts:
            - name: engine-data
              mountPath: /app/reports
            - name: engine-screenshots
              mountPath: /app/screenshots
          livenessProbe:
            httpGet:
              path: /health
              port: 5001
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 5001
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "2000m"
              memory: "2Gi"
      volumes:
        - name: engine-data
          persistentVolumeClaim:
            claimName: engine-data-pvc
        - name: engine-screenshots
          persistentVolumeClaim:
            claimName: engine-screenshots-pvc

---
# ── Engine Service ────────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: neurex-engine
  namespace: neurex
spec:
  selector:
    app: neurex-engine
  ports:
    - port: 5001
      targetPort: 5001

---
# ── Frontend Deployment ───────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neurex-frontend
  namespace: neurex
  labels:
    app: neurex-frontend
    tier: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: neurex-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: neurex-frontend
    spec:
      containers:
        - name: web
          image: ghcr.io/neurex-ai/neurex-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: NEXT_PUBLIC_API_BASE
              value: "https://bgtest.dev"
            - name: NEXT_PUBLIC_ENGINE_URL
              value: "https://bgtest.dev/engine"
            - name: NODE_ENV
              value: production
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

---
# ── Frontend Service ──────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: neurex-frontend
  namespace: neurex
spec:
  selector:
    app: neurex-frontend
  ports:
    - port: 3000
      targetPort: 3000

---
# ── AI Gateway Deployment ─────────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neurex-ai-gateway
  namespace: neurex
  labels:
    app: neurex-ai-gateway
    tier: ai-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: neurex-ai-gateway
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: neurex-ai-gateway
        tier: ai-gateway
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: ai-gateway
          image: ghcr.io/neurex-ai/neurex-ai-gateway:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: ENGINE_INTERNAL_KEY
              valueFrom:
                secretKeyRef:
                  name: neurex-secrets
                  key: engine-internal-key
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://neurex-otel-collector:4317"
            - name: OTEL_SERVICE_NAME
              value: "neurex-ai-gateway"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "deployment.environment=production"
            - name: LOG_FORMAT
              value: "json"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]

---
# ── AI Gateway Service ────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: neurex-ai-gateway
  namespace: neurex
spec:
  selector:
    app: neurex-ai-gateway
  ports:
    - port: 8080
      targetPort: 8080
      name: http

---
# ── Ingress (cert-manager + Let's Encrypt) ────────────────────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: neurex-ingress
  namespace: neurex
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
spec:
  tls:
    - hosts:
        - bgtest.dev
        - www.bgtest.dev
      secretName: neurex-tls
  rules:
    - host: bgtest.dev
      http:
        paths:
          - path: /api/
            pathType: Prefix
            backend:
              service:
                name: neurex-backend
                port:
                  number: 8000
          - path: /engine/
            pathType: Prefix
            backend:
              service:
                name: neurex-engine
                port:
                  number: 5001
          - path: /ai/
            pathType: Prefix
            backend:
              service:
                name: neurex-ai-gateway
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: neurex-frontend
                port:
                  number: 3000

---
# ── HorizontalPodAutoscaler — Backend ─────────────────────────────────────────
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: neurex-backend-hpa
  namespace: neurex
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: neurex-backend
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80

---
# ── HorizontalPodAutoscaler — AI Gateway ──────────────────────────────────────
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: neurex-ai-gateway-hpa
  namespace: neurex
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: neurex-ai-gateway
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80

---
# ── PersistentVolumeClaims ────────────────────────────────────────────────────
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: engine-data-pvc
  namespace: neurex
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: engine-screenshots-pvc
  namespace: neurex
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
