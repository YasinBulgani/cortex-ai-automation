apiVersion: v1
kind: Service
metadata:
  name: synthbank-service
  namespace: syntheticbank
  labels:
    app: synthbank-app
spec:
  type: ClusterIP
  selector:
    app: synthbank-app
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  sessionAffinity: None
