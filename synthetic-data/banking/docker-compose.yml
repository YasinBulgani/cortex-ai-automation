version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: synthbank
      POSTGRES_USER: synth
      POSTGRES_PASSWORD: synth
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
