Start all Docker services (postgres, redis, backend, engine).

Usage: /docker-up [service_name]

```bash
cd /Users/yasin_bulgan/Desktop/Cortex_Ai_Automation && docker compose up -d $ARGUMENTS && docker compose ps
```
