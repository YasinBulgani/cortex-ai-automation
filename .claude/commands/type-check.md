Run TypeScript type checking across the monorepo (apps/web).

Usage: /type-check

```bash
cd /Users/yasin_bulgan/Desktop/Cortex_Ai_Automation && npm run type-check --workspace apps/web 2>&1 | tail -40
```
