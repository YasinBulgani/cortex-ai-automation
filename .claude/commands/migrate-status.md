Show current Alembic migration status and pending migrations.

Usage: /migrate-status

```bash
cd /Users/yasin_bulgan/Desktop/Cortex_Ai_Automation/backend && alembic current && echo "---" && alembic history --verbose 2>&1 | head -30
```
