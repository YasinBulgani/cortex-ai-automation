[mutmut]
# Sadece saf-Python iş mantığını mutasyona uğrat — sürücü/UI değil
paths_to_mutate=engine/evals/scorer.py
tests_dir=engine/tests/unit/
runner=python -m pytest engine/tests/unit/test_scorer.py --noconftest -x -q --tb=no
dict_synonyms=T
